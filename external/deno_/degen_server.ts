// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               v3.19.1
// source: degen_server.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "npm:@bufbuild/protobuf@2.2.3/wire";
import { type CallContext, type CallOptions } from "npm:nice-grpc-common@2.0.2";

export const protobufPackage = "degen_server";

/** TODO: audit */
export interface NoncelessBlockHeader {
  version: Uint8Array;
  previousBlockHash: Uint8Array;
  merkleRoot: Uint8Array;
  timestamp: Uint8Array;
  /** Is equivalent to the bitcoin blockchain compact target value. */
  compactTarget: Uint8Array;
}

export interface NoncedBlockHeader {
  noncelessBlockHeader: NoncelessBlockHeader | undefined;
  nonce: Uint8Array;
}

export interface Event {
  eventId: string;
  eventType: string;
  eventData: Uint8Array;
  timestamp: number;
}

export interface GetEventsRequest {
  startTime: number;
  endTime: number;
  limit: number;
}

export interface GetEventsResponseOk {
  events: Event[];
}

export interface GetEventsResponseError {
  errorEnum: GetEventsResponseError_GetEventsResponseErrorEnum;
  errorMessage: string;
}

export enum GetEventsResponseError_GetEventsResponseErrorEnum {
  Unknown = 0,
  InvalidTimestamp = 1,
  InvalidLimit = 2,
  UNRECOGNIZED = -1,
}

export function getEventsResponseError_GetEventsResponseErrorEnumFromJSON(
  object: any,
): GetEventsResponseError_GetEventsResponseErrorEnum {
  switch (object) {
    case 0:
    case "Unknown":
      return GetEventsResponseError_GetEventsResponseErrorEnum.Unknown;
    case 1:
    case "InvalidTimestamp":
      return GetEventsResponseError_GetEventsResponseErrorEnum.InvalidTimestamp;
    case 2:
    case "InvalidLimit":
      return GetEventsResponseError_GetEventsResponseErrorEnum.InvalidLimit;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GetEventsResponseError_GetEventsResponseErrorEnum.UNRECOGNIZED;
  }
}

export function getEventsResponseError_GetEventsResponseErrorEnumToJSON(
  object: GetEventsResponseError_GetEventsResponseErrorEnum,
): string {
  switch (object) {
    case GetEventsResponseError_GetEventsResponseErrorEnum.Unknown:
      return "Unknown";
    case GetEventsResponseError_GetEventsResponseErrorEnum.InvalidTimestamp:
      return "InvalidTimestamp";
    case GetEventsResponseError_GetEventsResponseErrorEnum.InvalidLimit:
      return "InvalidLimit";
    case GetEventsResponseError_GetEventsResponseErrorEnum.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface GetEventsResponse {
  ok?: GetEventsResponseOk | undefined;
  error?: GetEventsResponseError | undefined;
}

export interface StreamingWorkClientMessage {
  challengeRequest?: StreamingWorkChallengeRequest | undefined;
  submissionRequest?: StreamingWorkSubmissionRequest | undefined;
}

export interface StreamingWorkServerMessage {
  challengeResponse?: StreamingWorkChallengeResponse | undefined;
  submissionResponse?: StreamingWorkSubmissionResponse | undefined;
  blockTemplateUpdate?: BlockTemplateUpdate | undefined;
}

export interface StreamingWorkChallengeRequest {
  minLeadingZeroCountU8: number;
  /**
   * TODO: audit min_solution_count_u32 > 0 usage; make this a OneOf instead of checking for 0 everywhere in code.
   * A min solution count is the minimum number of solutions that the client must find for a job. If this is 0, then let's run in continuous mode.
   */
  minSolutionCountU32: number;
  requestorServiceName: string;
}

export interface StreamingWorkChallengeResponse {
  jobId: string;
  noncelessBlockHeader:
    | NoncelessBlockHeader
    | undefined;
  /** TODO: maybe unnecessary */
  hashTargetLeadingZeroCountU8: number;
}

export interface StreamingWorkSubmissionRequest {
  jobId: string;
  noncedBlockHeaders: NoncedBlockHeader[];
}

export interface StreamingWorkSubmissionResponse {
  /** TODO: maybe unnecessary */
  status: StreamingWorkSubmissionResponse_Status;
  message: string;
}

export enum StreamingWorkSubmissionResponse_Status {
  ACCEPTED = 0,
  REJECTED = 1,
  OUTDATED = 2,
  ACCEPTED_AND_FOUND_BLOCK = 3,
  UNRECOGNIZED = -1,
}

export function streamingWorkSubmissionResponse_StatusFromJSON(object: any): StreamingWorkSubmissionResponse_Status {
  switch (object) {
    case 0:
    case "ACCEPTED":
      return StreamingWorkSubmissionResponse_Status.ACCEPTED;
    case 1:
    case "REJECTED":
      return StreamingWorkSubmissionResponse_Status.REJECTED;
    case 2:
    case "OUTDATED":
      return StreamingWorkSubmissionResponse_Status.OUTDATED;
    case 3:
    case "ACCEPTED_AND_FOUND_BLOCK":
      return StreamingWorkSubmissionResponse_Status.ACCEPTED_AND_FOUND_BLOCK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StreamingWorkSubmissionResponse_Status.UNRECOGNIZED;
  }
}

export function streamingWorkSubmissionResponse_StatusToJSON(object: StreamingWorkSubmissionResponse_Status): string {
  switch (object) {
    case StreamingWorkSubmissionResponse_Status.ACCEPTED:
      return "ACCEPTED";
    case StreamingWorkSubmissionResponse_Status.REJECTED:
      return "REJECTED";
    case StreamingWorkSubmissionResponse_Status.OUTDATED:
      return "OUTDATED";
    case StreamingWorkSubmissionResponse_Status.ACCEPTED_AND_FOUND_BLOCK:
      return "ACCEPTED_AND_FOUND_BLOCK";
    case StreamingWorkSubmissionResponse_Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface BlockTemplateUpdate {
  noncelessBlockHeader: NoncelessBlockHeader | undefined;
}

export interface GetBitcoinNetworkInfoRequest {
}

export interface GetBitcoinNetworkInfoResponseOk {
  blockHeight: number;
  /** Network difficulty as a floating point number */
  networkDifficulty: number;
}

export interface GetBitcoinNetworkInfoResponseError {
  errorEnum: GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum;
  errorMessage: string;
}

export enum GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum {
  Unknown = 0,
  BitcoinNodeNotConnected = 1,
  BitcoinNodeSyncing = 2,
  UNRECOGNIZED = -1,
}

export function getBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnumFromJSON(
  object: any,
): GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum {
  switch (object) {
    case 0:
    case "Unknown":
      return GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum.Unknown;
    case 1:
    case "BitcoinNodeNotConnected":
      return GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum.BitcoinNodeNotConnected;
    case 2:
    case "BitcoinNodeSyncing":
      return GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum.BitcoinNodeSyncing;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum.UNRECOGNIZED;
  }
}

export function getBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnumToJSON(
  object: GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum,
): string {
  switch (object) {
    case GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum.Unknown:
      return "Unknown";
    case GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum.BitcoinNodeNotConnected:
      return "BitcoinNodeNotConnected";
    case GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum.BitcoinNodeSyncing:
      return "BitcoinNodeSyncing";
    case GetBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnum.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface GetBitcoinNetworkInfoResponse {
  ok?: GetBitcoinNetworkInfoResponseOk | undefined;
  error?: GetBitcoinNetworkInfoResponseError | undefined;
}

function createBaseNoncelessBlockHeader(): NoncelessBlockHeader {
  return {
    version: new Uint8Array(0),
    previousBlockHash: new Uint8Array(0),
    merkleRoot: new Uint8Array(0),
    timestamp: new Uint8Array(0),
    compactTarget: new Uint8Array(0),
  };
}

export const NoncelessBlockHeader: MessageFns<NoncelessBlockHeader> = {
  encode(message: NoncelessBlockHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version.length !== 0) {
      writer.uint32(10).bytes(message.version);
    }
    if (message.previousBlockHash.length !== 0) {
      writer.uint32(18).bytes(message.previousBlockHash);
    }
    if (message.merkleRoot.length !== 0) {
      writer.uint32(26).bytes(message.merkleRoot);
    }
    if (message.timestamp.length !== 0) {
      writer.uint32(34).bytes(message.timestamp);
    }
    if (message.compactTarget.length !== 0) {
      writer.uint32(42).bytes(message.compactTarget);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NoncelessBlockHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNoncelessBlockHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.previousBlockHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.merkleRoot = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timestamp = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.compactTarget = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NoncelessBlockHeader {
    return {
      version: isSet(object.version) ? bytesFromBase64(object.version) : new Uint8Array(0),
      previousBlockHash: isSet(object.previousBlockHash)
        ? bytesFromBase64(object.previousBlockHash)
        : new Uint8Array(0),
      merkleRoot: isSet(object.merkleRoot) ? bytesFromBase64(object.merkleRoot) : new Uint8Array(0),
      timestamp: isSet(object.timestamp) ? bytesFromBase64(object.timestamp) : new Uint8Array(0),
      compactTarget: isSet(object.compactTarget) ? bytesFromBase64(object.compactTarget) : new Uint8Array(0),
    };
  },

  toJSON(message: NoncelessBlockHeader): unknown {
    const obj: any = {};
    if (message.version.length !== 0) {
      obj.version = base64FromBytes(message.version);
    }
    if (message.previousBlockHash.length !== 0) {
      obj.previousBlockHash = base64FromBytes(message.previousBlockHash);
    }
    if (message.merkleRoot.length !== 0) {
      obj.merkleRoot = base64FromBytes(message.merkleRoot);
    }
    if (message.timestamp.length !== 0) {
      obj.timestamp = base64FromBytes(message.timestamp);
    }
    if (message.compactTarget.length !== 0) {
      obj.compactTarget = base64FromBytes(message.compactTarget);
    }
    return obj;
  },

  create(base?: DeepPartial<NoncelessBlockHeader>): NoncelessBlockHeader {
    return NoncelessBlockHeader.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NoncelessBlockHeader>): NoncelessBlockHeader {
    const message = createBaseNoncelessBlockHeader();
    message.version = object.version ?? new Uint8Array(0);
    message.previousBlockHash = object.previousBlockHash ?? new Uint8Array(0);
    message.merkleRoot = object.merkleRoot ?? new Uint8Array(0);
    message.timestamp = object.timestamp ?? new Uint8Array(0);
    message.compactTarget = object.compactTarget ?? new Uint8Array(0);
    return message;
  },
};

function createBaseNoncedBlockHeader(): NoncedBlockHeader {
  return { noncelessBlockHeader: undefined, nonce: new Uint8Array(0) };
}

export const NoncedBlockHeader: MessageFns<NoncedBlockHeader> = {
  encode(message: NoncedBlockHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.noncelessBlockHeader !== undefined) {
      NoncelessBlockHeader.encode(message.noncelessBlockHeader, writer.uint32(10).fork()).join();
    }
    if (message.nonce.length !== 0) {
      writer.uint32(18).bytes(message.nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NoncedBlockHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNoncedBlockHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.noncelessBlockHeader = NoncelessBlockHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nonce = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NoncedBlockHeader {
    return {
      noncelessBlockHeader: isSet(object.noncelessBlockHeader)
        ? NoncelessBlockHeader.fromJSON(object.noncelessBlockHeader)
        : undefined,
      nonce: isSet(object.nonce) ? bytesFromBase64(object.nonce) : new Uint8Array(0),
    };
  },

  toJSON(message: NoncedBlockHeader): unknown {
    const obj: any = {};
    if (message.noncelessBlockHeader !== undefined) {
      obj.noncelessBlockHeader = NoncelessBlockHeader.toJSON(message.noncelessBlockHeader);
    }
    if (message.nonce.length !== 0) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    return obj;
  },

  create(base?: DeepPartial<NoncedBlockHeader>): NoncedBlockHeader {
    return NoncedBlockHeader.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NoncedBlockHeader>): NoncedBlockHeader {
    const message = createBaseNoncedBlockHeader();
    message.noncelessBlockHeader = (object.noncelessBlockHeader !== undefined && object.noncelessBlockHeader !== null)
      ? NoncelessBlockHeader.fromPartial(object.noncelessBlockHeader)
      : undefined;
    message.nonce = object.nonce ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEvent(): Event {
  return { eventId: "", eventType: "", eventData: new Uint8Array(0), timestamp: 0 };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.eventType !== "") {
      writer.uint32(18).string(message.eventType);
    }
    if (message.eventData.length !== 0) {
      writer.uint32(26).bytes(message.eventData);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.eventData = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Event {
    return {
      eventId: isSet(object.eventId) ? globalThis.String(object.eventId) : "",
      eventType: isSet(object.eventType) ? globalThis.String(object.eventType) : "",
      eventData: isSet(object.eventData) ? bytesFromBase64(object.eventData) : new Uint8Array(0),
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: Event): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.eventData.length !== 0) {
      obj.eventData = base64FromBytes(message.eventData);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create(base?: DeepPartial<Event>): Event {
    return Event.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Event>): Event {
    const message = createBaseEvent();
    message.eventId = object.eventId ?? "";
    message.eventType = object.eventType ?? "";
    message.eventData = object.eventData ?? new Uint8Array(0);
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseGetEventsRequest(): GetEventsRequest {
  return { startTime: 0, endTime: 0, limit: 0 };
}

export const GetEventsRequest: MessageFns<GetEventsRequest> = {
  encode(message: GetEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.startTime !== 0) {
      writer.uint32(8).int64(message.startTime);
    }
    if (message.endTime !== 0) {
      writer.uint32(16).int64(message.endTime);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.startTime = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.endTime = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEventsRequest {
    return {
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: GetEventsRequest): unknown {
    const obj: any = {};
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create(base?: DeepPartial<GetEventsRequest>): GetEventsRequest {
    return GetEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEventsRequest>): GetEventsRequest {
    const message = createBaseGetEventsRequest();
    message.startTime = object.startTime ?? 0;
    message.endTime = object.endTime ?? 0;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseGetEventsResponseOk(): GetEventsResponseOk {
  return { events: [] };
}

export const GetEventsResponseOk: MessageFns<GetEventsResponseOk> = {
  encode(message: GetEventsResponseOk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.events) {
      Event.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEventsResponseOk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEventsResponseOk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.events.push(Event.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEventsResponseOk {
    return { events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => Event.fromJSON(e)) : [] };
  },

  toJSON(message: GetEventsResponseOk): unknown {
    const obj: any = {};
    if (message.events?.length) {
      obj.events = message.events.map((e) => Event.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetEventsResponseOk>): GetEventsResponseOk {
    return GetEventsResponseOk.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEventsResponseOk>): GetEventsResponseOk {
    const message = createBaseGetEventsResponseOk();
    message.events = object.events?.map((e) => Event.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetEventsResponseError(): GetEventsResponseError {
  return { errorEnum: 0, errorMessage: "" };
}

export const GetEventsResponseError: MessageFns<GetEventsResponseError> = {
  encode(message: GetEventsResponseError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.errorEnum !== 0) {
      writer.uint32(8).int32(message.errorEnum);
    }
    if (message.errorMessage !== "") {
      writer.uint32(18).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEventsResponseError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEventsResponseError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.errorEnum = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEventsResponseError {
    return {
      errorEnum: isSet(object.errorEnum)
        ? getEventsResponseError_GetEventsResponseErrorEnumFromJSON(object.errorEnum)
        : 0,
      errorMessage: isSet(object.errorMessage) ? globalThis.String(object.errorMessage) : "",
    };
  },

  toJSON(message: GetEventsResponseError): unknown {
    const obj: any = {};
    if (message.errorEnum !== 0) {
      obj.errorEnum = getEventsResponseError_GetEventsResponseErrorEnumToJSON(message.errorEnum);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create(base?: DeepPartial<GetEventsResponseError>): GetEventsResponseError {
    return GetEventsResponseError.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEventsResponseError>): GetEventsResponseError {
    const message = createBaseGetEventsResponseError();
    message.errorEnum = object.errorEnum ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseGetEventsResponse(): GetEventsResponse {
  return { ok: undefined, error: undefined };
}

export const GetEventsResponse: MessageFns<GetEventsResponse> = {
  encode(message: GetEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== undefined) {
      GetEventsResponseOk.encode(message.ok, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      GetEventsResponseError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ok = GetEventsResponseOk.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = GetEventsResponseError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEventsResponse {
    return {
      ok: isSet(object.ok) ? GetEventsResponseOk.fromJSON(object.ok) : undefined,
      error: isSet(object.error) ? GetEventsResponseError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: GetEventsResponse): unknown {
    const obj: any = {};
    if (message.ok !== undefined) {
      obj.ok = GetEventsResponseOk.toJSON(message.ok);
    }
    if (message.error !== undefined) {
      obj.error = GetEventsResponseError.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<GetEventsResponse>): GetEventsResponse {
    return GetEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEventsResponse>): GetEventsResponse {
    const message = createBaseGetEventsResponse();
    message.ok = (object.ok !== undefined && object.ok !== null)
      ? GetEventsResponseOk.fromPartial(object.ok)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? GetEventsResponseError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseStreamingWorkClientMessage(): StreamingWorkClientMessage {
  return { challengeRequest: undefined, submissionRequest: undefined };
}

export const StreamingWorkClientMessage: MessageFns<StreamingWorkClientMessage> = {
  encode(message: StreamingWorkClientMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.challengeRequest !== undefined) {
      StreamingWorkChallengeRequest.encode(message.challengeRequest, writer.uint32(10).fork()).join();
    }
    if (message.submissionRequest !== undefined) {
      StreamingWorkSubmissionRequest.encode(message.submissionRequest, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamingWorkClientMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamingWorkClientMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.challengeRequest = StreamingWorkChallengeRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.submissionRequest = StreamingWorkSubmissionRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamingWorkClientMessage {
    return {
      challengeRequest: isSet(object.challengeRequest)
        ? StreamingWorkChallengeRequest.fromJSON(object.challengeRequest)
        : undefined,
      submissionRequest: isSet(object.submissionRequest)
        ? StreamingWorkSubmissionRequest.fromJSON(object.submissionRequest)
        : undefined,
    };
  },

  toJSON(message: StreamingWorkClientMessage): unknown {
    const obj: any = {};
    if (message.challengeRequest !== undefined) {
      obj.challengeRequest = StreamingWorkChallengeRequest.toJSON(message.challengeRequest);
    }
    if (message.submissionRequest !== undefined) {
      obj.submissionRequest = StreamingWorkSubmissionRequest.toJSON(message.submissionRequest);
    }
    return obj;
  },

  create(base?: DeepPartial<StreamingWorkClientMessage>): StreamingWorkClientMessage {
    return StreamingWorkClientMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamingWorkClientMessage>): StreamingWorkClientMessage {
    const message = createBaseStreamingWorkClientMessage();
    message.challengeRequest = (object.challengeRequest !== undefined && object.challengeRequest !== null)
      ? StreamingWorkChallengeRequest.fromPartial(object.challengeRequest)
      : undefined;
    message.submissionRequest = (object.submissionRequest !== undefined && object.submissionRequest !== null)
      ? StreamingWorkSubmissionRequest.fromPartial(object.submissionRequest)
      : undefined;
    return message;
  },
};

function createBaseStreamingWorkServerMessage(): StreamingWorkServerMessage {
  return { challengeResponse: undefined, submissionResponse: undefined, blockTemplateUpdate: undefined };
}

export const StreamingWorkServerMessage: MessageFns<StreamingWorkServerMessage> = {
  encode(message: StreamingWorkServerMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.challengeResponse !== undefined) {
      StreamingWorkChallengeResponse.encode(message.challengeResponse, writer.uint32(10).fork()).join();
    }
    if (message.submissionResponse !== undefined) {
      StreamingWorkSubmissionResponse.encode(message.submissionResponse, writer.uint32(18).fork()).join();
    }
    if (message.blockTemplateUpdate !== undefined) {
      BlockTemplateUpdate.encode(message.blockTemplateUpdate, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamingWorkServerMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamingWorkServerMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.challengeResponse = StreamingWorkChallengeResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.submissionResponse = StreamingWorkSubmissionResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.blockTemplateUpdate = BlockTemplateUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamingWorkServerMessage {
    return {
      challengeResponse: isSet(object.challengeResponse)
        ? StreamingWorkChallengeResponse.fromJSON(object.challengeResponse)
        : undefined,
      submissionResponse: isSet(object.submissionResponse)
        ? StreamingWorkSubmissionResponse.fromJSON(object.submissionResponse)
        : undefined,
      blockTemplateUpdate: isSet(object.blockTemplateUpdate)
        ? BlockTemplateUpdate.fromJSON(object.blockTemplateUpdate)
        : undefined,
    };
  },

  toJSON(message: StreamingWorkServerMessage): unknown {
    const obj: any = {};
    if (message.challengeResponse !== undefined) {
      obj.challengeResponse = StreamingWorkChallengeResponse.toJSON(message.challengeResponse);
    }
    if (message.submissionResponse !== undefined) {
      obj.submissionResponse = StreamingWorkSubmissionResponse.toJSON(message.submissionResponse);
    }
    if (message.blockTemplateUpdate !== undefined) {
      obj.blockTemplateUpdate = BlockTemplateUpdate.toJSON(message.blockTemplateUpdate);
    }
    return obj;
  },

  create(base?: DeepPartial<StreamingWorkServerMessage>): StreamingWorkServerMessage {
    return StreamingWorkServerMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamingWorkServerMessage>): StreamingWorkServerMessage {
    const message = createBaseStreamingWorkServerMessage();
    message.challengeResponse = (object.challengeResponse !== undefined && object.challengeResponse !== null)
      ? StreamingWorkChallengeResponse.fromPartial(object.challengeResponse)
      : undefined;
    message.submissionResponse = (object.submissionResponse !== undefined && object.submissionResponse !== null)
      ? StreamingWorkSubmissionResponse.fromPartial(object.submissionResponse)
      : undefined;
    message.blockTemplateUpdate = (object.blockTemplateUpdate !== undefined && object.blockTemplateUpdate !== null)
      ? BlockTemplateUpdate.fromPartial(object.blockTemplateUpdate)
      : undefined;
    return message;
  },
};

function createBaseStreamingWorkChallengeRequest(): StreamingWorkChallengeRequest {
  return { minLeadingZeroCountU8: 0, minSolutionCountU32: 0, requestorServiceName: "" };
}

export const StreamingWorkChallengeRequest: MessageFns<StreamingWorkChallengeRequest> = {
  encode(message: StreamingWorkChallengeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.minLeadingZeroCountU8 !== 0) {
      writer.uint32(8).int32(message.minLeadingZeroCountU8);
    }
    if (message.minSolutionCountU32 !== 0) {
      writer.uint32(16).int32(message.minSolutionCountU32);
    }
    if (message.requestorServiceName !== "") {
      writer.uint32(26).string(message.requestorServiceName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamingWorkChallengeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamingWorkChallengeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.minLeadingZeroCountU8 = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minSolutionCountU32 = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requestorServiceName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamingWorkChallengeRequest {
    return {
      minLeadingZeroCountU8: isSet(object.minLeadingZeroCountU8) ? globalThis.Number(object.minLeadingZeroCountU8) : 0,
      minSolutionCountU32: isSet(object.minSolutionCountU32) ? globalThis.Number(object.minSolutionCountU32) : 0,
      requestorServiceName: isSet(object.requestorServiceName) ? globalThis.String(object.requestorServiceName) : "",
    };
  },

  toJSON(message: StreamingWorkChallengeRequest): unknown {
    const obj: any = {};
    if (message.minLeadingZeroCountU8 !== 0) {
      obj.minLeadingZeroCountU8 = Math.round(message.minLeadingZeroCountU8);
    }
    if (message.minSolutionCountU32 !== 0) {
      obj.minSolutionCountU32 = Math.round(message.minSolutionCountU32);
    }
    if (message.requestorServiceName !== "") {
      obj.requestorServiceName = message.requestorServiceName;
    }
    return obj;
  },

  create(base?: DeepPartial<StreamingWorkChallengeRequest>): StreamingWorkChallengeRequest {
    return StreamingWorkChallengeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamingWorkChallengeRequest>): StreamingWorkChallengeRequest {
    const message = createBaseStreamingWorkChallengeRequest();
    message.minLeadingZeroCountU8 = object.minLeadingZeroCountU8 ?? 0;
    message.minSolutionCountU32 = object.minSolutionCountU32 ?? 0;
    message.requestorServiceName = object.requestorServiceName ?? "";
    return message;
  },
};

function createBaseStreamingWorkChallengeResponse(): StreamingWorkChallengeResponse {
  return { jobId: "", noncelessBlockHeader: undefined, hashTargetLeadingZeroCountU8: 0 };
}

export const StreamingWorkChallengeResponse: MessageFns<StreamingWorkChallengeResponse> = {
  encode(message: StreamingWorkChallengeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.noncelessBlockHeader !== undefined) {
      NoncelessBlockHeader.encode(message.noncelessBlockHeader, writer.uint32(18).fork()).join();
    }
    if (message.hashTargetLeadingZeroCountU8 !== 0) {
      writer.uint32(24).int32(message.hashTargetLeadingZeroCountU8);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamingWorkChallengeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamingWorkChallengeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.noncelessBlockHeader = NoncelessBlockHeader.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hashTargetLeadingZeroCountU8 = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamingWorkChallengeResponse {
    return {
      jobId: isSet(object.jobId) ? globalThis.String(object.jobId) : "",
      noncelessBlockHeader: isSet(object.noncelessBlockHeader)
        ? NoncelessBlockHeader.fromJSON(object.noncelessBlockHeader)
        : undefined,
      hashTargetLeadingZeroCountU8: isSet(object.hashTargetLeadingZeroCountU8)
        ? globalThis.Number(object.hashTargetLeadingZeroCountU8)
        : 0,
    };
  },

  toJSON(message: StreamingWorkChallengeResponse): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.noncelessBlockHeader !== undefined) {
      obj.noncelessBlockHeader = NoncelessBlockHeader.toJSON(message.noncelessBlockHeader);
    }
    if (message.hashTargetLeadingZeroCountU8 !== 0) {
      obj.hashTargetLeadingZeroCountU8 = Math.round(message.hashTargetLeadingZeroCountU8);
    }
    return obj;
  },

  create(base?: DeepPartial<StreamingWorkChallengeResponse>): StreamingWorkChallengeResponse {
    return StreamingWorkChallengeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamingWorkChallengeResponse>): StreamingWorkChallengeResponse {
    const message = createBaseStreamingWorkChallengeResponse();
    message.jobId = object.jobId ?? "";
    message.noncelessBlockHeader = (object.noncelessBlockHeader !== undefined && object.noncelessBlockHeader !== null)
      ? NoncelessBlockHeader.fromPartial(object.noncelessBlockHeader)
      : undefined;
    message.hashTargetLeadingZeroCountU8 = object.hashTargetLeadingZeroCountU8 ?? 0;
    return message;
  },
};

function createBaseStreamingWorkSubmissionRequest(): StreamingWorkSubmissionRequest {
  return { jobId: "", noncedBlockHeaders: [] };
}

export const StreamingWorkSubmissionRequest: MessageFns<StreamingWorkSubmissionRequest> = {
  encode(message: StreamingWorkSubmissionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    for (const v of message.noncedBlockHeaders) {
      NoncedBlockHeader.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamingWorkSubmissionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamingWorkSubmissionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.noncedBlockHeaders.push(NoncedBlockHeader.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamingWorkSubmissionRequest {
    return {
      jobId: isSet(object.jobId) ? globalThis.String(object.jobId) : "",
      noncedBlockHeaders: globalThis.Array.isArray(object?.noncedBlockHeaders)
        ? object.noncedBlockHeaders.map((e: any) => NoncedBlockHeader.fromJSON(e))
        : [],
    };
  },

  toJSON(message: StreamingWorkSubmissionRequest): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.noncedBlockHeaders?.length) {
      obj.noncedBlockHeaders = message.noncedBlockHeaders.map((e) => NoncedBlockHeader.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<StreamingWorkSubmissionRequest>): StreamingWorkSubmissionRequest {
    return StreamingWorkSubmissionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamingWorkSubmissionRequest>): StreamingWorkSubmissionRequest {
    const message = createBaseStreamingWorkSubmissionRequest();
    message.jobId = object.jobId ?? "";
    message.noncedBlockHeaders = object.noncedBlockHeaders?.map((e) => NoncedBlockHeader.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStreamingWorkSubmissionResponse(): StreamingWorkSubmissionResponse {
  return { status: 0, message: "" };
}

export const StreamingWorkSubmissionResponse: MessageFns<StreamingWorkSubmissionResponse> = {
  encode(message: StreamingWorkSubmissionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamingWorkSubmissionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamingWorkSubmissionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamingWorkSubmissionResponse {
    return {
      status: isSet(object.status) ? streamingWorkSubmissionResponse_StatusFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: StreamingWorkSubmissionResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = streamingWorkSubmissionResponse_StatusToJSON(message.status);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<StreamingWorkSubmissionResponse>): StreamingWorkSubmissionResponse {
    return StreamingWorkSubmissionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamingWorkSubmissionResponse>): StreamingWorkSubmissionResponse {
    const message = createBaseStreamingWorkSubmissionResponse();
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseBlockTemplateUpdate(): BlockTemplateUpdate {
  return { noncelessBlockHeader: undefined };
}

export const BlockTemplateUpdate: MessageFns<BlockTemplateUpdate> = {
  encode(message: BlockTemplateUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.noncelessBlockHeader !== undefined) {
      NoncelessBlockHeader.encode(message.noncelessBlockHeader, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BlockTemplateUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlockTemplateUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.noncelessBlockHeader = NoncelessBlockHeader.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BlockTemplateUpdate {
    return {
      noncelessBlockHeader: isSet(object.noncelessBlockHeader)
        ? NoncelessBlockHeader.fromJSON(object.noncelessBlockHeader)
        : undefined,
    };
  },

  toJSON(message: BlockTemplateUpdate): unknown {
    const obj: any = {};
    if (message.noncelessBlockHeader !== undefined) {
      obj.noncelessBlockHeader = NoncelessBlockHeader.toJSON(message.noncelessBlockHeader);
    }
    return obj;
  },

  create(base?: DeepPartial<BlockTemplateUpdate>): BlockTemplateUpdate {
    return BlockTemplateUpdate.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BlockTemplateUpdate>): BlockTemplateUpdate {
    const message = createBaseBlockTemplateUpdate();
    message.noncelessBlockHeader = (object.noncelessBlockHeader !== undefined && object.noncelessBlockHeader !== null)
      ? NoncelessBlockHeader.fromPartial(object.noncelessBlockHeader)
      : undefined;
    return message;
  },
};

function createBaseGetBitcoinNetworkInfoRequest(): GetBitcoinNetworkInfoRequest {
  return {};
}

export const GetBitcoinNetworkInfoRequest: MessageFns<GetBitcoinNetworkInfoRequest> = {
  encode(_: GetBitcoinNetworkInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBitcoinNetworkInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBitcoinNetworkInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetBitcoinNetworkInfoRequest {
    return {};
  },

  toJSON(_: GetBitcoinNetworkInfoRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetBitcoinNetworkInfoRequest>): GetBitcoinNetworkInfoRequest {
    return GetBitcoinNetworkInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetBitcoinNetworkInfoRequest>): GetBitcoinNetworkInfoRequest {
    const message = createBaseGetBitcoinNetworkInfoRequest();
    return message;
  },
};

function createBaseGetBitcoinNetworkInfoResponseOk(): GetBitcoinNetworkInfoResponseOk {
  return { blockHeight: 0, networkDifficulty: 0 };
}

export const GetBitcoinNetworkInfoResponseOk: MessageFns<GetBitcoinNetworkInfoResponseOk> = {
  encode(message: GetBitcoinNetworkInfoResponseOk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.blockHeight !== 0) {
      writer.uint32(8).int32(message.blockHeight);
    }
    if (message.networkDifficulty !== 0) {
      writer.uint32(17).double(message.networkDifficulty);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBitcoinNetworkInfoResponseOk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBitcoinNetworkInfoResponseOk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.blockHeight = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.networkDifficulty = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBitcoinNetworkInfoResponseOk {
    return {
      blockHeight: isSet(object.blockHeight) ? globalThis.Number(object.blockHeight) : 0,
      networkDifficulty: isSet(object.networkDifficulty) ? globalThis.Number(object.networkDifficulty) : 0,
    };
  },

  toJSON(message: GetBitcoinNetworkInfoResponseOk): unknown {
    const obj: any = {};
    if (message.blockHeight !== 0) {
      obj.blockHeight = Math.round(message.blockHeight);
    }
    if (message.networkDifficulty !== 0) {
      obj.networkDifficulty = message.networkDifficulty;
    }
    return obj;
  },

  create(base?: DeepPartial<GetBitcoinNetworkInfoResponseOk>): GetBitcoinNetworkInfoResponseOk {
    return GetBitcoinNetworkInfoResponseOk.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBitcoinNetworkInfoResponseOk>): GetBitcoinNetworkInfoResponseOk {
    const message = createBaseGetBitcoinNetworkInfoResponseOk();
    message.blockHeight = object.blockHeight ?? 0;
    message.networkDifficulty = object.networkDifficulty ?? 0;
    return message;
  },
};

function createBaseGetBitcoinNetworkInfoResponseError(): GetBitcoinNetworkInfoResponseError {
  return { errorEnum: 0, errorMessage: "" };
}

export const GetBitcoinNetworkInfoResponseError: MessageFns<GetBitcoinNetworkInfoResponseError> = {
  encode(message: GetBitcoinNetworkInfoResponseError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.errorEnum !== 0) {
      writer.uint32(8).int32(message.errorEnum);
    }
    if (message.errorMessage !== "") {
      writer.uint32(18).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBitcoinNetworkInfoResponseError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBitcoinNetworkInfoResponseError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.errorEnum = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBitcoinNetworkInfoResponseError {
    return {
      errorEnum: isSet(object.errorEnum)
        ? getBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnumFromJSON(object.errorEnum)
        : 0,
      errorMessage: isSet(object.errorMessage) ? globalThis.String(object.errorMessage) : "",
    };
  },

  toJSON(message: GetBitcoinNetworkInfoResponseError): unknown {
    const obj: any = {};
    if (message.errorEnum !== 0) {
      obj.errorEnum = getBitcoinNetworkInfoResponseError_GetBitcoinNetworkInfoErrorEnumToJSON(message.errorEnum);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create(base?: DeepPartial<GetBitcoinNetworkInfoResponseError>): GetBitcoinNetworkInfoResponseError {
    return GetBitcoinNetworkInfoResponseError.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBitcoinNetworkInfoResponseError>): GetBitcoinNetworkInfoResponseError {
    const message = createBaseGetBitcoinNetworkInfoResponseError();
    message.errorEnum = object.errorEnum ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseGetBitcoinNetworkInfoResponse(): GetBitcoinNetworkInfoResponse {
  return { ok: undefined, error: undefined };
}

export const GetBitcoinNetworkInfoResponse: MessageFns<GetBitcoinNetworkInfoResponse> = {
  encode(message: GetBitcoinNetworkInfoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== undefined) {
      GetBitcoinNetworkInfoResponseOk.encode(message.ok, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      GetBitcoinNetworkInfoResponseError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBitcoinNetworkInfoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBitcoinNetworkInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ok = GetBitcoinNetworkInfoResponseOk.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = GetBitcoinNetworkInfoResponseError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBitcoinNetworkInfoResponse {
    return {
      ok: isSet(object.ok) ? GetBitcoinNetworkInfoResponseOk.fromJSON(object.ok) : undefined,
      error: isSet(object.error) ? GetBitcoinNetworkInfoResponseError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: GetBitcoinNetworkInfoResponse): unknown {
    const obj: any = {};
    if (message.ok !== undefined) {
      obj.ok = GetBitcoinNetworkInfoResponseOk.toJSON(message.ok);
    }
    if (message.error !== undefined) {
      obj.error = GetBitcoinNetworkInfoResponseError.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<GetBitcoinNetworkInfoResponse>): GetBitcoinNetworkInfoResponse {
    return GetBitcoinNetworkInfoResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBitcoinNetworkInfoResponse>): GetBitcoinNetworkInfoResponse {
    const message = createBaseGetBitcoinNetworkInfoResponse();
    message.ok = (object.ok !== undefined && object.ok !== null)
      ? GetBitcoinNetworkInfoResponseOk.fromPartial(object.ok)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? GetBitcoinNetworkInfoResponseError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

export type DegenServiceDefinition = typeof DegenServiceDefinition;
export const DegenServiceDefinition = {
  name: "DegenService",
  fullName: "degen_server.DegenService",
  methods: {
    getEvents: {
      name: "GetEvents",
      requestType: GetEventsRequest,
      requestStream: false,
      responseType: GetEventsResponse,
      responseStream: false,
      options: {},
    },
    /**
     * The caller is some other business service related to getting work.
     * It should have knowledge/control for how difficult the work is to do.
     * For now, we will do just 1 job per streaming work channel. Therefore, every service will likely have many streaming work channels to the degen service.
     */
    streamingWork: {
      name: "StreamingWork",
      requestType: StreamingWorkClientMessage,
      requestStream: true,
      responseType: StreamingWorkServerMessage,
      responseStream: true,
      options: {},
    },
    /** Get current information about the Bitcoin network */
    getBitcoinNetworkInfo: {
      name: "GetBitcoinNetworkInfo",
      requestType: GetBitcoinNetworkInfoRequest,
      requestStream: false,
      responseType: GetBitcoinNetworkInfoResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface DegenServiceImplementation<CallContextExt = {}> {
  getEvents(request: GetEventsRequest, context: CallContext & CallContextExt): Promise<DeepPartial<GetEventsResponse>>;
  /**
   * The caller is some other business service related to getting work.
   * It should have knowledge/control for how difficult the work is to do.
   * For now, we will do just 1 job per streaming work channel. Therefore, every service will likely have many streaming work channels to the degen service.
   */
  streamingWork(
    request: AsyncIterable<StreamingWorkClientMessage>,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<StreamingWorkServerMessage>>;
  /** Get current information about the Bitcoin network */
  getBitcoinNetworkInfo(
    request: GetBitcoinNetworkInfoRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetBitcoinNetworkInfoResponse>>;
}

export interface DegenServiceClient<CallOptionsExt = {}> {
  getEvents(request: DeepPartial<GetEventsRequest>, options?: CallOptions & CallOptionsExt): Promise<GetEventsResponse>;
  /**
   * The caller is some other business service related to getting work.
   * It should have knowledge/control for how difficult the work is to do.
   * For now, we will do just 1 job per streaming work channel. Therefore, every service will likely have many streaming work channels to the degen service.
   */
  streamingWork(
    request: AsyncIterable<DeepPartial<StreamingWorkClientMessage>>,
    options?: CallOptions & CallOptionsExt,
  ): AsyncIterable<StreamingWorkServerMessage>;
  /** Get current information about the Bitcoin network */
  getBitcoinNetworkInfo(
    request: DeepPartial<GetBitcoinNetworkInfoRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetBitcoinNetworkInfoResponse>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
